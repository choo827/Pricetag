import React, { FC } from 'react';
import './App.css';

export const App: FC = props => (
  <div className="App">
    <div>PriceTag</div>
    <button type="button" onClick={() => {}}>
      Click me
    </button>
  </div>
);
